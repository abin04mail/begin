from tkinter import *

top = Tk()
top.title('Введите домашний адрес')
top.geometry('500x300')

# btn = Button(top, text='Value', command=select)
# btn.pack(anchor=CENTER)

label = Label(top)
label.pack()

top.mainloop()